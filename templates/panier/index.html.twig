{% extends 'base.html.twig' %}

{% block title %}Mon Panier!{% endblock %}

{% block stylesheets %}{{ encore_entry_link_tags('css_cart') }}{{ parent() }}{% endblock %}

{% block body %}



        <div class="article-wrapper">
        {% for article in articles %}

                <div class="card">
                    <div class="container">
                        <h4><b>{{ article.article.intitule }}</b><br>
                            {{ article.article.prix }}€
                        </h4>

                        <p>{{ article.article.description }} </p>
                        <div class="options">
                            <button>
                                <a href="{{ path('app_article_stock_show', {'id': article.article.id}) }}">Voir en Boutique</a>
                            </button>
                            <button>
                                &plus;
                            </button>
                            <div class="quantity">
                                {{ article.quantity}}
                            </div>
                            <button id="suppCart">
                                &minus;
                                <div hidden id="idItem">{{ article.article.id }}</div>
                            </button>
                        </div>
                    </div>
                </div>
            </a>
        {% else %}
            <tr>
                <td colspan="5">Aucun Article dans le Panier</td>
            </tr>
        {% endfor %}
        </div>
    <div class="totalPrice"><h1>{{ prix }}</h1></div>

{% endblock %}
