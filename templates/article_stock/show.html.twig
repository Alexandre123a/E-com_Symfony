{% extends 'base.html.twig' %}
{% block stylesheets %} {{ encore_entry_link_tags('app') }} {{ parent() }} {% endblock %}
{% block title %}ArticleStock{% endblock %}

{% block body %}
    <h1>ArticleStock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Intitule</th>
                <td>{{ article_stock.intitule }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ article_stock.prix }} €</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article_stock.description }}</td>
            </tr>
        </tbody>
    </table>

    {% if is_granted('ROLE_USER') %}
        <a href="{{ path('app_panier') }}">
            <button>Ajouter au panier</button>
        </a>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}

    <a href="{{ path('app_article_stock_index') }}">back to list</a>

    <a href="{{ path('app_article_stock_edit', {'id': article_stock.id}) }}">edit</a>

    {{ include('article_stock/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
